package ocp;

import java.io.*;



public class IO_Serializable5 extends Dog {
	
	
	/*
	 * To create a reference variable for an instance of a new Java 6 Console object, 
	 * class(es) from which package(s) must be used? 
	 * (Choose all that apply.)
	 * 
	 * A.  java.io
	 * 
	 * B.  java.awt
	 * 
	 * C.  java.lang
	 * 
	 * D.  java.text
	 * 
	 * E.  java.util
	 * 
	 * F.  javax.swing
	 * 
	 * A and C are correct. A line of code such as Console c = System.console(); creates a new instance of java.io.Console, 
	 * and the System class is in the java.lang package.
	 * 
	 * B, D, E, and F are incorrect based on the above.
	 * 
	 * คำอธิบาย:
	 * - A: ถูกต้อง เพราะ `java.io.Console` เป็นคลาสที่ใช้สำหรับอินเตอร์เฟซคอนโซลใน Java ซึ่งอยู่ในแพ็กเกจ `java.io`
	 * - B: ไม่ถูกต้อง เพราะ `java.awt` เป็นแพ็กเกจที่ใช้สำหรับสร้างกราฟิกส์ และไม่เกี่ยวข้องกับคอนโซล
	 * - C: ถูกต้อง เพราะ `System` ซึ่งใช้ในการสร้างอ็อบเจ็กต์คอนโซลนั้นอยู่ในแพ็กเกจ `java.lang`
	 * - D: ไม่ถูกต้อง เพราะ `java.text` เป็นแพ็กเกจที่เกี่ยวข้องกับการจัดการข้อความและรูปแบบวันที่ ไม่เกี่ยวข้องกับคอนโซล
	 * - E: ไม่ถูกต้อง เพราะ `java.util` เป็นแพ็กเกจที่ใช้สำหรับเครื่องมือและโครงสร้างข้อมูล เช่น `List`, `Map`, และ `Set` ไม่ใช่สำหรับคอนโซล
	 * - F: ไม่ถูกต้อง เพราะ `javax.swing` เป็นแพ็กเกจที่ใช้สำหรับ GUI ไม่ใช่คอนโซล
	 */


}

